<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.screens.AdvertisingActivity">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPagerAdvertisement"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayoutAdvertisement"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/nextButton"
        app:layout_constraintLeft_toRightOf="@id/prefButton"
        app:layout_constraintRight_toLeftOf="@id/nextButton"
        app:layout_constraintTop_toTopOf="@id/nextButton"
        app:tabBackground="@drawable/bg_tab"
        app:tabGravity="center"
        app:tabIndicator="@drawable/bg_tab_indicator"
        app:tabIndicatorColor="#00FF2B"
        app:tabIndicatorGravity="center"
        app:tabMode="fixed"
        app:tabPaddingEnd="6dp"
        app:tabPaddingStart="6dp" />

    <Button
        android:id="@+id/prefButton"
        style="@style/Button.Action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:background="@drawable/button_dialog_done_back"
        android:text="@string/back"
        android:textColor="#FFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent" />

    <Button
        android:id="@+id/nextButton"
        style="@style/Button.Action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:background="@drawable/button_dialog_done_back"
        android:text="@string/next"
        android:textColor="#FFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>